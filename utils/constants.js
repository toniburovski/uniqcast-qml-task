const constants = Object.freeze({
    // api
    API_URL: "http://devel.uniqcast.com:3001",

    // auth
    AUTH_ENDPOINT:      '/auth/local',

    // channels
    CHANNELS_ENDPOINT:  '/channels',
});
